#include <iostream>

using namespace std;

int moja_funkcija(int n)
{
  if (n > 0)
  {
    return (n * n * n) * 2;
  }
  else
    return -404;
}

int main()
{
  int a, b;
  
  a = moja_funkcija(100);
  cout << a << endl;
  
  b = moja_funkcija(-100);
  cout << b << endl;
  return 0;
}
